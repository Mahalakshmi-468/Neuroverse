[project]
name = "neuroverse"
version = "0.2.0"
description = "NeuroVerse — an AI-powered interactive storytelling prototype"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.110",
    "pydantic>=2.6",
    "anthropic>=0.40",
    "python-dotenv>=1.0",
]

[tool.fastapi]
entrypoint = "backend.app:app"
